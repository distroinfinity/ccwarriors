{
  "name": "claude-warriors",
  "version": "0.1.0",
  "description": "CLI for Claude Warriors — sync your Claude Code costs and climb the leaderboard",
  "type": "module",
  "bin": {
    "ccwarriors": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0"
  }
}
